/* =====================================================
   EUROPE LIVE — MAIN
===================================================== */

document.addEventListener(
    "DOMContentLoaded",
    () => {

        initTVEngine();

        initCookies();

        initChat();

        initCategories();

    }
);
